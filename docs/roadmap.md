So we can use the fastify instend of express to increase the performance
Can we implement suppose if someone enters url and we do some testing like send some real traffic and check whether this url is valid or not
